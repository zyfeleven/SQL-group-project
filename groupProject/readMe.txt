We have four folders containing all codes for populating, triggers and views.
ACCESS: the database access file
populate: codes for populating, it's the same file as the one prof uploaded on brightspace
triggers: all triggers codes, divided by table name. 
	For example, character.txt contains all triggers on table character.

views: the views we created for reports.